// tuple example
// an array with fixed length and data type

// tuples are rearly used, for example working with CSV files

// video 33

type Drink = [string, boolean, number];

const coffee: Drink = ["black", false, 7];
const tea: Drink = ["green", false, 10];
const soda: Drink = ["clear", true, 330];

console.log(coffee);
console.log(tea);
console.log(soda);